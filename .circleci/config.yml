version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: git-jira-integrations/git-jira-integrations
    steps:
      - run: echo "Hi I'm on Runners!"